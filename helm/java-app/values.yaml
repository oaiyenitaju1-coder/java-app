image:
  repository: horla1/java-app
  tag: latest
  pullPolicy: Always

replicaCount: 1

service:
  type: NodePort
  port: 8080
  nodePort: 30080

resources:
  requests:
    memory: "256Mi"
    cpu: "250m"
  limits:
    memory: "512Mi"
    cpu: "500m"
